# Placeholders so the frontend boots before the user provides real Base44
# credentials via the dashboard Secrets page. Real values in /run/base44/app.env
# (delivered last) always override these.
VITE_BASE44_APP_ID=placeholder
VITE_BASE44_APP_BASE_URL=https://placeholder.base44.app
